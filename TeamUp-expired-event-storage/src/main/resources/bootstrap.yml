spring:
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  
  datasource:
    platform: mysql
    url: jdbc:mysql://localhost:3306/teamup_expired_event_storage?serverTimezone=UTC
    username: root
    password: Srduicukzh1
    driverClassName: com.mysql.cj.jdbc.Driver
    
  